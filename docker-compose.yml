version: "3.9"
services:
  nnds:
    build: .
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
    command: python scripts/run_pipeline.py --video data/sample_data/traffic_video.mp4 --imgsz 640

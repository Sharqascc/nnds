[pytest]
addopts = --cov=src --cov-report=term-missing --cov-branch --cov-fail-under=84
markers = 
	        integration: heavy tests requiring real data or external services
	        slow: tests that take long to run
	        smoke: smoke tests for heavy modules
	    
testpaths = tests


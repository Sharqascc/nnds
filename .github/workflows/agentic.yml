name: Agentic Auto-Fix

on:
  schedule:
    - cron: '0 0 * * *'   # daily at midnight UTC
  workflow_dispatch:       # manual trigger

jobs:
  agentic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agentic/auto-fixes
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov pytest-timeout hypothesis ruff mypy requests groq
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run agentic fixer
        env:
          GROQ_API_KEY: ${{ secrets.GROUQ_API_KEY }}
        run: |
          python scripts/agentic_fix.py
